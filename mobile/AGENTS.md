# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

Pinned to SDK 54 deliberately (not the latest) to match what's currently
installable through the Expo Go app on real devices — check before bumping
this without also confirming Expo Go itself supports the new SDK.
